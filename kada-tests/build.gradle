dependencies {
    compile (
            project(":kada-core"),
            project(":kada-template"),
            project(":kada-original")
    )

    testCompile (
            "junit:junit:4.12"
    )
}