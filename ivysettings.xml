<?xml version="1.0" encoding="UTF-8"?>
<ivysettings>
    <property name="repository.dir" value="F:/repository" />
    <!-- <property name="ivy.default.ivy.user.dir" value="http://10.21.3.56:8081/nexus/content/groups/public" /> -->

    <!-- 配置 -->
    <property name="ivy.local.default.root" value="${repository.dir}" override="false" />
    <property name="ivy.local.default.ivy.pattern" value="[artifact]-[revision].[ext]" override="false" />
    <property name="ivy.local.default.artifact.pattern" value="[artifact]-[revision].[ext]" override="false" />
<!--     <property name="ivy.local.default.ivy.pattern" value="[organisation]/[module]/[revision]/[type]s/[artifact].[ext]" override="false" />
    <property name="ivy.local.default.artifact.pattern" value="[organisation]/[module]/[revision]/[type]s/[artifact].[ext]" override="false" /> -->
    
    <!-- 远程仓库配置 -->
    <!-- <property name="ivy.shared.default.root"             value="${ivy.default.ivy.user.dir}" override="false"/>
    <property name="ivy.shared.default.ivy.pattern"      value="[organisation]/[module]/[revision]/[type]s/[artifact].[ext]" override="false"/>
    <property name="ivy.shared.default.artifact.pattern" value="[organisation]/[module]/[revision]/[type]s/[artifact].[ext]" override="false"/> -->
    
    <settings defaultResolver="main" />
    <resolvers>
        <!-- 本地仓库 -->
        <filesystem name="local">
            <ivy pattern="${ivy.local.default.root}/${ivy.local.default.ivy.pattern}" />
            <artifact pattern="${ivy.local.default.root}/${ivy.local.default.artifact.pattern}" />
        </filesystem>
        
        <!-- 远程仓库 -->
        <!-- <filesystem name="shared">
            <ivy pattern="${ivy.shared.default.root}/${ivy.shared.default.ivy.pattern}" />
            <artifact pattern="${ivy.shared.default.root}/${ivy.shared.default.artifact.pattern}" />
        </filesystem> -->
        
        <!-- 公网仓库 -->
        <ibiblio name="public" m2compatible="true"/>
        <!-- 获取链 -->
        <chain name="main" returnFirst="true">
          <resolver ref="local"/>
          <resolver ref="public"/>
        </chain>
    </resolvers>

    <caches defaultCacheDir="${ivy.local.default.root}/ivy-cache" />
</ivysettings>
