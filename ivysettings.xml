<?xml version="1.0" encoding="UTF-8"?>
<ivysettings>

    <!-- 配置 -->
    <!--<property name="ivy.local.default.ivy.pattern" value="[artifact]-[revision].[ext]" override="false" />
    <property name="ivy.local.default.artifact.pattern" value="[artifact]-[revision].[ext]" override="false" />-->
    <property name="ivy.local.default.root" value="C:\Users\lenovo\.m2\repository" override="false" />
    <property name="ivy.local.default.ivy.pattern" value="[organisation]/[module]/[revision]/[artifact]-[revision].[ext]" override="false" />
    <property name="ivy.local.default.artifact.pattern" value="[organisation]/[module]/[revision]/[artifact]-[revision].[ext]" override="false" />
    
    <!-- 远程仓库配置 -->
    <property name="ivy.shared.default.root"             value="http://172.30.3.176:8081/nexus/content/groups/public" override="false"/>
    <property name="ivy.shared.default.ivy.pattern"      value="[organisation]/[module]/[revision]/[artifact]-[revision].[ext]" override="false"/>
    <property name="ivy.shared.default.artifact.pattern" value="[organisation]/[module]/[revision]/[artifact]-[revision].[ext]" override="false"/>
    
    <settings defaultResolver="main" />
    <resolvers>
        <!-- 本地仓库 -->
        <filesystem name="local" m2compatible="true" >
            <ivy pattern="${ivy.local.default.root}/${ivy.local.default.ivy.pattern}"/>
            <artifact pattern="${ivy.local.default.root}/${ivy.local.default.artifact.pattern}"/>
        </filesystem>

        <!-- 远程仓库 -->
        <url name="shared" m2compatible="true" >
            <ivy pattern="${ivy.shared.default.root}/${ivy.shared.default.ivy.pattern}"/>
            <artifact pattern="${ivy.shared.default.root}/${ivy.shared.default.artifact.pattern}" />
        </url>

        <!-- 公网仓库 -->
        <!--<ibiblio name="public" m2compatible="true"/>-->
        <!-- 获取链 -->
        <chain name="main" dual="true">
            <resolver ref="local"/>
            <resolver ref="shared"/>
            <!--<resolver ref="public"/>-->
        </chain>
    </resolvers>

    <!--<caches defaultCacheDir="${ivy.local.default.root}/ivy-cache" />-->
</ivysettings>
